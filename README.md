# factory status

One file, rewritten by `factory dashboard --push`:
the redacted public state of the software factory, fetched client-side
by the live board. Counts and shape only — no task text, no message
bodies, no issue or PR titles.

**Nothing here is edited by hand.** It is generated; the live originals
are on the machine that runs the factory.
